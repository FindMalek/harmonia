# Database migrations workflow
# Runs Drizzle schema migrations and db-ops data migrations when relevant files change on main.
#
# Required secret:
#   PRODUCTION_DATABASE_URL - Production PostgreSQL connection string.
#   Add it in: GitHub repo → Settings → Secrets and variables → Actions.

name: Drizzle Migrate

on:
  push:
    branches: [main]
    paths:
      - "packages/db/src/migrations/**"
      - "packages/db/src/schema/**"
      - "packages/db/drizzle.config.ts"
      - "packages/db/package.json"
      - "packages/db-ops/**"
  workflow_dispatch:

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run schema migrations
        working-directory: packages/db
        env:
          HARMONIA_DATABASE_URL: ${{ secrets.PRODUCTION_DATABASE_URL }}
        run: pnpm db:migrate

      - name: Run db-ops migrations
        working-directory: packages/db-ops
        env:
          HARMONIA_DATABASE_URL: ${{ secrets.PRODUCTION_DATABASE_URL }}
        run: pnpm db:ops:migrate
