import type { NextConfig } from "next";

/**
 * Minimal Next.js config for shadcn CLI framework detection.
 * This package is a library consumed by Next.js apps; it does not run Next.js.
 */
const config: NextConfig = {};

export default config;
