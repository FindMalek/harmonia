---
description: Dashboard app conventions — component structure, data fetching, state, naming
globs:
  - "apps/dashboard/**"
alwaysApply: false
---

# Dashboard App (`apps/dashboard`)

## Directory Structure

```
apps/dashboard/src/
  app/(dashboard)/        # authenticated routes
  app/(onboarding)/       # onboarding flow
  app/login/
  hooks/                  # cross-cutting client hooks (prefix use-, co-locate only when owned by one component)
  components/
    app/                  # feature-specific (prefixed dashboard-{feature}-{role})
    layout/               # page wrappers, nav, providers
    shared/               # truly reusable (empty-state, error-state, page-loader)
  shared/
    api/                  # orpc client, auth-client, session.server, query-keys, error-handler
    lib/
      organize/           # store.ts (+ sessionStorage helpers for active run id) + controller.hook.ts
      pipeline/           # pipeline.util.ts (pure helpers + TanStack/orpc prefetch query presets), controller.hook.ts
      playlists/          # store.ts + controller.hook.ts
      tracks/             # store.ts + controller.hook.ts
      clusters/           # store.ts + controller.hook.ts
      onboarding/         # store.ts + controller.hook.ts (sync stream)
  lib/
    utils.ts              # cn() only
    env.ts                # env re-export only
```

## Component Conventions

- File: `dashboard-{feature}-{role}.tsx` (kebab-case)
- Export: `PascalCase` matching the filename
- Always co-locate a skeleton: `export function DashboardXyzSkeleton() { ... }`
- Extract inline components out of page files into `components/app/`

## Data Fetching

- Server state: TanStack Query v5 via `orpc` client (`@/shared/api/orpc`)
- Shared prefetch/query alignment for `pipeline.getAll` / `pipeline.stats`: use `pipelineGetAllQueryOptions` / `pipelineStatsQueryOptions` from `@/shared/lib/pipeline/pipeline.util` in RSC prefetch and in `controller.hook` — do not add a separate `*-query-options.ts` file
- Use controller hooks from `@/shared/lib/{domain}/controller.hook` — they combine queries, mutations, and store access
- Invalidate via `queryKeys.*` from `@/shared/api/query-keys`
- Import `toastError` from `@/shared/api/error-handler` for copy-button error toasts

## Client State

- Per-domain Zustand stores in `shared/lib/{domain}/store.ts` — one store per domain, no cross-domain monolith
- Persisted UI selection tied to a domain store (e.g. active pipeline run id in sessionStorage) lives in that domain’s `store.ts` next to `setState` — no separate `*-session.ts` helpers
- Access via controller hooks; do not import domain stores directly in page files
- No `useState` for cross-component state — use the domain store via its controller

## Toast Conventions

- `toast.error()` for failures
- No `toast.success()` on pipeline launch (drawer opening is feedback enough)
- Error toasts always provide a "Copy" action for the error message

## Container Patterns

- **`DashboardSettingsSection`** (`components/app/dashboard-settings-section.tsx`) — bordered container with uppercase section label. Use for settings, metadata, or any grouped-rows page section.
- **`DashboardSettingsRow`** — label left, value right with `border-b`. Use inside `DashboardSettingsSection`.
- **`DashboardSettingsNavRow`** — tappable row with label, subtitle, and `chevronRight`. Use for preference rows that open a sub-action.
- **`DashboardPlaylistDetailSectionLabel`** — standalone label + `Separator`. Use when only a section header is needed without a bordered container.
- Inline row dividers: `border-b py-4 last:border-b-0` directly on each row element.

## Navigation

- Routes defined in `@harmonia/common/utils/routes` as `DASHBOARD_ROUTES`
- Use `Link` from `next/link` for all navigation — no `window.location.href` except hard redirects
